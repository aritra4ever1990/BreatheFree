<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>BreatheFree</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- CSS -->
  <link rel="stylesheet" href="css/style.css" />

  <!-- Chart.js -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>

<body>
  <!-- HEADER -->
  <header class="top-bar">
    🫁 <strong>BreatheFree</strong>
    <span id="clock"></span>
  </header>

  <!-- STATS -->
  <section class="stats">
    <div class="card orange"><h1 id="streak">0</h1><p>🔥 Streak (days)</p></div>
    <div class="card red"><h1 id="smokedToday">0</h1><p>🚭 Smoked Today</p></div>
    <div class="card green"><h1 id="wallet">₹0</h1><p>💰 Wallet</p></div>
    <div class="card purple"><h1 id="awards">₹0</h1><p>🏆 Awards</p></div>
  </section>

  <!-- ACTION BUTTONS -->
  <section class="actions">
    <button id="smokeBtn" class="btn red">🚬 I Smoked</button>
    <button id="craveBtn" class="btn orange">😡 Craving</button>
    <button id="stopBtn" class="btn gray">■ Stop</button>
  </section>

<!-- CRAVING TIMER -->
<div id="cravingTimer"></div>

  <!-- MESSAGE -->
  <div id="message" class="message"></div>

  <!-- AI COACH -->
  <div class="coach">
    <h3>🧠 Coach Insight</h3>
    <div id="aiInsight"></div>
    <div id="dangerHours"></div>
  </div>

  <!-- CHART -->
  <section class="chart-section">
    <h3>📈 Progress Tracker (Hourly)</h3>
    <canvas id="progressChart"></canvas>
  </section>

  <!-- HEATMAP -->
  <section>
    <h3>🌡️ Smoking Heatmap</h3>
    <div id="heatmap" class="heatmap"></div>
  </section>

  <!-- JS (IMPORTANT: defer) -->
  <script src="js/app.js" defer></script>
</body>
</html>
